{
  "name": "open-prompt-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsc-watch -p tsconfig.json --skipLibCheck --onSuccess './build/dev.sh'",
    "build": "rm -rf dist/ && tsc -p tsconfig.json && tscpaths -p tsconfig.json -s ./src -o ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@koa/cors": "^4.0.0",
    "@types/node": "^18.15.11",
    "koa": "^2.14.2",
    "koa-joi-router": "^8.0.0",
    "lodash": "^4.17.21",
    "configly": "^5.0.2",
    "js-yaml": "^4.1.0",
    "tencentcloud-sdk-nodejs": "^4.0.369"
  },
  "devDependencies": {
    "source-map-support": "^0.5.21",
    "tsc-watch": "^4.5.0",
    "tsconfig-paths": "^3.11.0",
    "tscpaths": "0.0.9",
    "typescript": "^4.0.0"
  }
}
